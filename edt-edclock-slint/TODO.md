# TODO — edt-edclock-slint

- [ ] Older-glibc `.deb` (build in Ubuntu 22.04 container when Docker is available).
- [ ] Install `rust-clippy` / `rustfmt` and add them to `setup.sh` optionally.
- [ ] Calendar event markers.
- [ ] Multi-partition disk line (MEMORY.md).
- [ ] Decide whether root `package-lock.json` drift should be committed on its own.
